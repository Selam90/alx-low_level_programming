#include <stdio.h>
/* main - Entry 
 * Return - Always 0
 */
int main(void)
{
	char c = 'a';
	/* spell alphbet with out q
	 * and e
	 */
	while 
		(c <= 'z')
		{
			if (c != 'q' && c != 'e')
				putchar(c);
			c++;
		}
	putchar('\n');
			return (0);
		}
