#include <stdio.h>
/*
 * main - entry point
 * Return: always 0
 */
int main(void)
{
	/*
	 * function - display 1-10
	 */
	int a;
	for ( a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	putchar('\n');
	return (0);
}
