#include "main.h"
#include <string.h>
/**
 * is_palindrome - check palindrom
 * @s: string
 * @i: rev
 * Return: 1 or 0
 */
int is_palindrome(char *s)
{
	int i, l;

	if ( s ==  '\0')
	return (0)
}
	s++;
	if (s[l] != s[i])
			{
				return (0);
					else
					return (1);
			}

